run:
  timeout: 5m

issues:
  exclude-rules:
    # Test infrastructure files spawn real processes; unchecked errors are acceptable
    - path: tests/
      linters:
        - errcheck
        - unused
        - staticcheck
    # _test.go files: allow unchecked errors and suppress false-positive static analysis
    - path: _test\.go
      linters:
        - errcheck
        - staticcheck
